Установка Docker для Kali Linux

Для начала обновим список доступных пакетов

sudo apt update

Далее установим docker.io

sudo apt install -y docker.io

Проверим, что всё работает

sudo docker

Затем разрешим запуск контейнеров текущему пользователю

sudo usermod -aG docker $USER

Осталось выйти из своей сессии или перезагрузить машину, чтобы изменения применились.
Наиболее используемые опции в контейнере Docker:

-ti – teletype (эмуляция входа в контейнер) и interactive (будем в эмуляции, пока сами не выйдем)
-e – прописать переменные окружения 
-d – запустить в фоновом режиме (демоном)
--link – подключение по сети двух контейнеров, legacy функция
-p – используется для проброса портов
Наиболее используемые команды Docker:

docker pull имя образа – скачивание образа
docker ps – используется для просмотра активных контейнеров
docker run * -v /home/mount/data:/var/lib/mysql/data – примонтировать директорию на хосте в docker; в Docker Compose сначала указывается docker путь, потом хост путь
docker network ls – показать сети docker
docker network inspect [имя сети] – посмотреть конфигурацию сети
docker rm [имя] – удалить контейнер
docker rmi [имя] – удалить image
docker-compose down остановка docker-compose контейнеров
docker rmi $(docker images -q) – удаление образов
docker stop $(docker ps -a -q)– остановка контейнеров
docker rm $(docker ps -a -q)  – удаление остановленных контейнеров
docker container prune – новая функция удаления контейнеров
docker network prune  – удаление сетей
docker system prune – полный clean up контейнеров

Про другие команды можно почитать тут
Лабораторные по Docker - Play with Docker

Play with Docker - это интерактивная игровая площадка, которая позволяет вам запускать команды Docker на терминале Linux без необходимости предварительной локальной загрузки.

В этом пошаговом руководстве вы узнаете, как создавать образы, запускать контейнеры, использовать тома для сохранения данных и монтирования в исходном коде, а также определять свое приложение с помощью Docker Compose. Вы даже узнаете о нескольких продвинутых темах, таких как создание сетей и лучшие практики.

    Войдите в https://labs.play-with-docker.com/ для доступа к вашему терминалу
    Введите следующую команду в вашем терминале: docker run -dp 80:80 docker/getting-started:pwd
    Дождитесь запуска контейнера и нажмите на "open port"
    Успехов!

Дополнительные материалы о Docker

Лабораторная работа: введение в Docker с нуля. Ваш первый микросервис - https://habr.com/ru/articles/346634/

Руководство по Docker Compose для начинающих - https://habr.com/ru/companies/ruvds/articles/450312/
